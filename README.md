# Транслятор и модель процессора
- ФИО: Мыц Иван Евгеньевич
- Группа: P3210

Вариант:
lisp | cisc | harv | hw | tick | binary | trap | port | pstr | prob1 | pipeline

## Язык программирования

Разработанный язык представляет собой минималистичный диалект Lisp. 

### Синтаксис

Синтаксис языка в форме Бэкуса-Наура:

```ebnf
<program> ::= <form> <program> | <form>
<form> ::= <defun> | <defvar> | <definterrupt> | <expression>

<defun> ::= "(" "defun" <identifier> "(" <arg-list> ")" <expression-list> ")"
<defvar> ::= "(" "defvar" <identifier> <expression> ")"
<definterrupt> ::= "(" "definterrupt" <integer> <expression-list> ")"

<arg-list> ::= <identifier> <arg-list> | <empty>
<expression-list> ::= <expression> <expression-list> | <expression>

<expression> ::= <integer>
               | <string>
               | <identifier>
               | <function-call>
               | <if-expr>
               | <setq-expr>
               | <loop-expr>
               | <print-expr>
               | <progn-expr>
               | <math-expr>

<function-call> ::= "(" <identifier> <expr-list> ")"
<expr-list> ::= <expression> <expr-list> | <empty>

<if-expr> ::= "(" "if" <expression> <expression> <expression> ")"
<setq-expr> ::= "(" "setq" <identifier> <expression> ")"
<loop-expr> ::= "(" "loop" <expression> <expression-list> ")"
<print-expr> ::= "(" "print" <expression> ")"
<progn-expr> ::= "(" "progn" <expression-list> ")"

<math-expr> ::= "(" <operator> <expression> <expression> ")"
<operator> ::= "+" | "-" | "*" | "/" | "=" | "<" | ">" | "mod"

<identifier> ::= [a-zA-Z_][a-zA-Z0-9_-]*
<integer> ::= [-]? [0-9]+
<string> ::= '"' [^"]* '"'
```

### Семантика

В данном диалекте реализована парадигма **«всё есть выражение»**. Любая конструкция, включая условные операторы, циклы и присваивания, при вычислении возвращает значение, которое может быть использовано как аргумент для других выражений.

**Примеры корректного кода:**

- Выражение `if` возвращает результат вычисленной ветви:
  ```lisp
  (print (if (= 1 x) "T" "F"))
  ```
- Выражение `setq` выполняет присваивание и возвращает присвоенное значение:
  ```lisp
  (setq x (if (= 1 x) 2 3))
  ```
- Использование результата `setq` на месте:
  ```lisp
  (print (setq x 13))
  ```
- Выражение `loop` возвращает результат последнего вычисленного выражения в теле цикла:
  ```lisp
  (setq x (loop (< i 10) 
                (setq i (+ i 1)) 
                (+ i 100)))
  ```
- Рекурсивное вычисление N-го числа Фибоначчи
  ```lisp
    (defun fib (n)
    (if (< n 2)
        n
        (+ (fib (- n 1))
            (fib (- n 2)))))

    (print (fib 10))
  ```
- Итеративное вычисление факториала
  ```lisp
    (defvar result 1)
    (defvar n 5)

    (print 
    (progn
        (loop (> n 0)
        (setq result (* result n))
        (setq n (- n 1)))
        result))
  ```
#### Стратегия вычислений

Eager (applicative order), слева направо: в форме `(f a b c)` сначала вычисляются `a`, `b`, `c` именно в указанном порядке, потом вызывается `f`. Ленивости и макросов нет. Специальные формы (`if`, `loop`, `setq`, `progn`, `defun`, `defvar`, `definterrupt`) обрабатываются транслятором отдельно — у них собственные правила вычисления аргументов; всё остальное — это обычный вызов функции с предварительным вычислением всех аргументов.

#### Типы и литералы

Типизация динамическая. Поддерживается два типа значений:

- **Integer** — 32-битное знаковое число. Литералы: `42`, `-7`, `0`. Арифметика (`+`, `-`, `*`, `/`, `mod`) и сравнения (`<`, `>`, `=`) определены только для целых.
- **String** — неизменяемая строка. Литералы: `"hello"`. Допустимые операции: передача в `print` и сравнение на равенство через `=`. Физически хранится в data-memory как `pstr` — слово длины, затем `L` машинных слов по одному символу.

Истинность в `if` и `loop`: `0` — ложь, любое другое целое — истина. Сравнения возвращают `1` (истина) или `0` (ложь). Проверка типов при трансляции не выполняется — за корректность отвечает программист.

#### Области видимости

Лексические, ровно два уровня — глобальный и текущий вызов функции:

- `defvar x e` создаёт глобальную переменную `x` со значением `e`. Глобалы видны отовсюду.
- Параметры `defun` — локальные на время вызова, перекрывают глобал того же имени.
- `setq x e` присваивает значение **уже существующей** переменной: сначала ищется локальная, потом глобальная. Если `x` нигде не объявлена — ошибка трансляции.
- Вложенных `defun`, анонимных функций и замыканий нет.
- Рекурсия (включая взаимную) разрешена: имя, объявленное через `defun`, видно сразу после объявления, в том числе в собственном теле.

#### Возвращаемые значения

Реализована классическая парадигма «всё есть выражение»: любая форма что-то возвращает, и результат можно передать дальше.

| Форма | Возвращает |
| --- | --- |
| `defvar` / `defun` / `definterrupt` | `0` (используются ради побочного эффекта) |
| `setq x e` | значение `e` |
| `if p t e` | значение взятой ветви |
| `loop p body...` | значение последнего выражения тела на последней итерации; `0`, если тело не выполнилось ни разу |
| `progn e1 ... en` | значение `en` |
| `print e` | значение `e` |
| `(f a1 ... an)` | то, что вернула функция `f` (последнее выражение её тела) |

#### Прерывания

`(definterrupt N body...)` регистрирует тело как обработчик прерывания с вектором `N`. Тело компилируется как отдельная процедура, её адрес записывается в ячейку `N` таблицы векторов в памяти данных. При наступлении прерывания процессор сохраняет `PC` и `SR` на стек, гасит флаг `IE`, передаёт управление по адресу из таблицы. В конце тела транслятор автоматически вставляет `IRET`, который восстанавливает `PC` и `SR` (а вместе с `SR` — и `IE`). Для общения с портами внутри обработчика используются инструкции `IN` и `OUT` (см. раздел «Система команд»).

#### Отображение выражений на регистры и память

Стратегия размещения транслятором максимально прямолинейная:

- **Целочисленный литерал** вкладывается в инструкцию как непосредственный операнд. Если значение укладывается в диапазон −128..127, транслятор устанавливает бит `S=1` в дескрипторе и помещает значение в поле `imm8` — слово расширения не требуется. Иначе используется полный `#imm32` со словом расширения.
- **Строковый литерал** один раз помещается в data-memory как `pstr` (длина + символы). Транслятор генерирует встроенную подпрограмму `__print_str`, которая использует режим автоинкремента `[Rn]+` для эффективного посимвольного вывода строки без раздувания кода.
- **Глобальная переменная** (`defvar`) — именованная ячейка в data-memory; доступ через абсолютный режим `[imm32]`.
- **Параметр функции** размещается на стеке. Доступ через смещение от `FP`: `[FP + imm]`. Для функций с числом параметров до ~60 смещение укладывается в `imm8` (бит `S=1`), и доступ кодируется одним словом без расширения. Кадр строится как `[saved FP, return PC, argN, ..., arg1]`.
- **Промежуточные результаты вычисления выражений** вычисляются через стек и регистры `R0`-`R5`. Левое подвыражение вычисляется в `R0` и сохраняется на стек `PUSH R0`. Правое вычисляется в `R0`. Затем выполняется операция с использованием режима постинкремента: `ADD R0, [SP]+`. Это позволяет снять аргумент со стека и выполнить арифметику за одну инструкцию, избегая лишних `POP`.
- **Логические выражения** (`<`, `>`, `=`) не используют ветвлений. Для материализации логического значения транслятор использует инструкции условного присваивания (`SETL`, `SETG`, `SETE`), которые записывают `1` (истина) или `0` (ложь) напрямую в регистр `R0`.
- **Возвращаемое значение функции** всегда передаётся в `R0`.

## Организация памяти

Архитектура памяти построена по модели **Гарвардской архитектуры**:

- память команд и память данных разделены;
- обе памяти однопортовые;
- машинное слово: `32` бита;
- адресация линейная, по машинным словам, а не по байтам.

### Регистры

Доступны два независимых банка регистров:

- `R0`-`R5` — рабочие регистры (используются транслятором);
- `R6`-`R13` — зарезервированы для будущих расширений;
- `R14` (`FP`) — кадровый указатель;
- `R15` (`SP`) — стековый указатель;
- `PC` — счётчик команд;
- `SR` — статусный регистр с флагами `Z`, `N`, `IE`.

### Память команд

Память команд доступна только на чтение в рантайме.

- `0x0000+` - код программы.

Таблица векторов прерываний хранится не здесь, а в памяти данных (см. ниже).

### Память данных

Память данных доступна на чтение и запись.

- `0x0000-0x00FF` - таблица векторов прерываний (IVT): в ячейке `N` лежит адрес обработчика вектора `N` в памяти команд;
- далее - статическая область: глобальные переменные, константы и `pstr`-строки;
- `pstr` хранится как `L`, затем `L` машинных слов, по одному символу в каждом;
- высокие адреса - стек данных и возвратов.

Стек используется для:

- передачи аргументов;
- сохранения `PC` при `CALL` и прерываниях;
- хранения локальных переменных.

## Система команд

Процессор ориентирован на **CISC**-модель:

- переменная длина инструкций (от 1 до N машинных слов);
- операции могут обращаться прямо к памяти данных;
- поддерживаются операции с нефиксированным числом операндов (`INVOKE`, `PUSHM`, `POPM`);
- ввод-вывод реализован через порты.

### Режимы адресации и дескрипторы операндов

В двухоперандных инструкциях (формат `C2`) каждый операнд кодируется строго **8-битным дескриптором**. Это позволяет конвейеру (Instruction Decode) за один такт определять длину инструкции.

Формат дескриптора: `[Режим: 3 бита] [Регистр: 4 бита] [S: 1 бит]`.

Поле `Регистр` адресует все 16 регистров единого банка `R0`-`R15` (4 бита, значения `0..15`). Это согласовано с форматом C1, где регистры тоже кодируются 4 битами.

Бит **S (Short)** управляет источником значения операнда. Если `S=0`, режимы `001`/`010`/`011` читают полное 32-битное слово расширения как обычно. Если `S=1`, вместо слова расширения используется поле `imm8` из младшего байта `[7:0]` слова инструкции (знакорасширяется до 32 бит). Положение `imm8` в `[7:0]` **фиксировано для всех форматов** (и C2, и C3) и не зависит от того, какому дескриптору принадлежит короткое значение. Только один из двух операндов может иметь `S=1`, так как поле `imm8` в инструкции одно.

| Режим (bin) | Мнемоника | Синтаксис | S=0 | S=1 | Назначение |
| :---------: | --- | --- | :-: | :-: | --- |
| `000` | **Регистровый** | `Rn` | — | — | Работа с промежуточными данными и указателями |
| `001` | **Непосредственный** | `#imm` | слово расширения (imm32) | `imm8` из `[7:0]` | Целочисленные литералы |
| `010` | **Абсолютный** | `[imm]` | слово расширения (imm32) | `imm8` из `[7:0]` | Доступ к глобальным переменным (`defvar`) |
| `011` | **База + Смещение** | `[Rn + imm]` | слово расширения (imm32) | `imm8` из `[7:0]` | Доступ к аргументам (`[FP + offset]`) |
| `100` | **Постинкремент** | `[Rn]+` | — | — | Чтение со стека (`[SP]+`) и перебор строк |

Это позволяет кодировать часто встречающиеся паттерны без слова расширения: `ADD R0, #1` (маленькая константа, диапазон −128..127), `MOV R0, [FP+2]` (доступ к аргументу с малым смещением) — каждая такая инструкция занимает ровно 1 машинное слово вместо 2.

Оптимизация конвейера: из архитектуры исключены режимы с предекрементом и сложной индексной арифметикой, так как они вызывают структурные конфликты (Data Hazards) в ALU.

### Таблица кодов операций (Opcode Map)

Пространство кодов разделено на четыре группы:

#### Группа 1: Переменное число операндов (CISC-вызовы и контекст)

Разработаны специально для оптимизации трансляции Lisp и работы с прерываниями. Чтобы не создавать задержек (stalls) в конвейере, эти команды принимают только регистры, поэтому их длина фиксирована и легко предсказуема декодером.

| Мнемоника | Код | Формат | Описание |
| --- | --- | --- | --- |
| `INVOKE` | `0x10` | `C1` | `INVOKE func, count, arg1...argN`. ControlUnit входит в аппаратный цикл: на каждом такте декрементирует `SP` и записывает очередной регистр-аргумент в `D-Mem[SP]` (в обратном порядке, `N` тактов). Затем сохраняет `PC` на стек и загружает `PC` из слова расширения (`func`). Заменяет `N` отдельных `PUSH` + `CALL`. |
| `PUSHM` | `0x11` | `C1` | `PUSHM count, R1...Rn`. ControlUnit в цикле: для каждого регистра декрементирует `SP` и записывает значение в `D-Mem[SP]` (`N` тактов). |
| `POPM` | `0x12` | `C1` | `POPM count, R1...Rn`. ControlUnit в цикле: для каждого регистра читает `D-Mem[SP]` и инкрементирует `SP` (`N` тактов). |

**Пролог и эпилог функции.** `INVOKE` выполняет только передачу аргументов и переход — сохранение и восстановление кадра стека (`FP`) выполняется транслятором вручную. Генерируемый пролог: `PUSH FP` / `MOV FP, SP`. Эпилог: `MOV SP, FP` / `POP FP` / `RET`.

**INVOKE vs CALL.** `INVOKE` (группа 1) используется для Lisp-вызовов с аргументами — он аппаратно формирует стек параметров. `CALL` (группа 3) — простой переход с сохранением `PC`, без передачи аргументов; используется транслятором для внутренних подпрограмм (`__print_str` и подобных).

#### Группа 2: Фиксированное число операндов (ALU и Пересылки)

| Мнемоника | Код | Формат | Описание |
| --- | --- | --- | --- |
| `MOV` | `0x20` | `C2` | `dst = src` (Копирование данных или адресов) |
| `ADD` | `0x21` | `C2` | `dst = dst + src` |
| `SUB` | `0x22` | `C2` | `dst = dst - src` |
| `MUL` | `0x23` | `C2` | `dst = dst * src` |
| `DIV` | `0x24` | `C2` | `dst = dst / src` |
| `MOD` | `0x25` | `C2` | `dst = dst % src` |
| `CMP` | `0x26` | `C2` | Сравнение, установка флагов `Z`, `N` |
| `IN` | `0x27` | `C2` | `IN port, dst` |
| `OUT` | `0x28` | `C2` | `OUT src, port` |
| `SETL` | `0x29` | `C3` | `dst = (N == 1) ? 1 : 0` (Set if Less) |
| `SETG` | `0x2A` | `C3` | `dst = (Z == 0 && N == 0) ? 1 : 0` (Set if Greater) |
| `SETE` | `0x2B` | `C3` | `dst = (Z == 1) ? 1 : 0` (Set if Equal) |

#### Группа 3: Ветвления

Ветвления используют 24-битное знаковое смещение относительно адреса следующей команды.

| Мнемоника | Код | Формат | Описание |
| --- | --- | --- | --- |
| `JMP` | `0x30` | `B` | Безусловный переход |
| `BEQ` | `0x31` | `B` | Переход, если `Z = 1` |
| `BNE` | `0x32` | `B` | Переход, если `Z = 0` (Not Equal, ложь в Lisp) |
| `BLT` | `0x33` | `B` | Переход, если `N = 1` |
| `BGT` | `0x34` | `B` | Переход, если `Z = 0` и `N = 0` |
| `CALL` | `0x35` | `B` | Вызов функции (без параметров) |

#### Группа 4: Системные (Безоперандные)

| Мнемоника | Код | Формат | Описание |
| --- | --- | --- | --- |
| `RET` | `0x40` | `A` | Возврат из функции |
| `IRET` | `0x41` | `A` | Возврат из прерывания |
| `EI` | `0x42` | `A` | Разрешить прерывания |
| `DI` | `0x43` | `A` | Запретить прерывания |
| `HALT` | `0x44` | `A` | Остановка процессора |

### Бинарное кодирование

Инструкция считывается процессором по мере декодирования. Машинное слово — 32 бита.

#### Формат A. Безоперандные команды

| Биты | Смысл |
| --- | --- |
| `[31:24]` | Опкод операции |
| `[23:0]` | Нули |

#### Формат B. Ветвления и вызовы (PC-Relative)

| Биты | Смысл |
| --- | --- |
| `[31:24]` | Опкод операции |
| `[23:0]` | 24-битное знаковое смещение относительно адреса **следующей** команды (`PC_next`) |

#### Формат C1. Группа 1 (Переменное число операндов: INVOKE, PUSHM, POPM)

Операндами выступают только регистры, что избавляет конвейер от сложных вычислений адреса. В первое слово помещается до 4 регистров. Если `count > 4`, считывается следующее машинное слово с оставшимися регистрами.

| Биты | Смысл |
| --- | --- |
| `[31:24]` | Опкод (`INVOKE`, `PUSHM` или `POPM`) |
| `[23:16]` | `count` (число операндов, от `1` до `12`) |
| `[15:12]`, `[11:8]`, `[7:4]`, `[3:0]` | 4-битные идентификаторы регистров (операнды 1..4) |
| *(Слово 2)* | *(Опционально)* Идентификаторы регистров (операнды 5..12), если `count > 4` |
| *(Слово расширения)* | Только для `INVOKE`: 32-битный абсолютный адрес функции `func` |

#### Формат C3. Однооперандные команды (SETL, SETG, SETE)

| Биты | Смысл |
| --- | --- |
| `[31:24]` | 8-битный код операции |
| `[23:16]` | 8-битный дескриптор операнда (приёмник): `[mode:3][reg:4][S:1]` |
| `[15:8]` | Резерв (`0x00`) |
| `[7:0]` | `imm8` — короткое значение (используется, если `S=1`; иначе `0x00`) |

Инструкции `SETcc` читают флаги из `SR` и записывают `1` или `0` в приёмник. Второго операнда нет — результат определяется только состоянием флагов после предшествующей `CMP`.

> **Единое положение `imm8`.** Короткое значение (`S=1`) всегда лежит в младшем байте `[7:0]`
> инструкции — независимо от формата (C2 или C3) и от того, какому дескриптору оно принадлежит.
> Это упрощает извлечение: знакорасширитель всегда берёт `IR[7:0]`, отдельный мультиплексор позиции
> не нужен.

#### Формат C2. Общий формат для группы 2 (Два операнда)

| Биты | Смысл |
| --- | --- |
| `[31:24]` | 8-битный код операции |
| `[23:16]` | 8-битный дескриптор 1-го операнда (приёмник): `[mode:3][reg:4][S:1]` |
| `[15:8]` | 8-битный дескриптор 2-го операнда (источник): `[mode:3][reg:4][S:1]` |
| `[7:0]` | `imm8` — короткое непосредственное значение (используется, если у одного из операндов `S=1`; иначе `0x00`) |

Логика декодирования:

- Если оба дескриптора имеют `S=0` — поведение классическое: режимы `001`/`010`/`011` читают 32-битное слово расширения из потока инструкций.
- Если один из дескрипторов имеет `S=1` — его значение берётся из `imm8[7:0]` (знакорасширяется до 32 бит), слово расширения для него не читается. Диапазон: −128..127.
- `S=1` допускается только у одного операнда (поле `imm8` одно). Транслятор гарантирует это статически.

### Прерывания

Механизм `trap` работает так:

1. прерывание проверяется на границе команд при активном `IE`; команды, уже находящиеся в OF/EX и MEM/WB, дотекают до конца (точное прерывание), младшие стадии сбрасываются;
2. адрес первой невыполненной команды (`PC`), а затем `SR` аппаратно сохраняются в стек;
3. `IE` сбрасывается, `PC` загружается из таблицы векторов;
4. обработчик прерывания (тело `definterrupt`), сгенерированный транслятором, начинается с инструкции `PUSHM count, R0...R5` для защиты контекста прерванной программы;
5. перед `IRET` транслятор вставляет `POPM` для восстановления сохранённых регистров; `IRET` снимает со стека `SR`, затем `PC`.

## Транслятор

<!-- TODO: Описать:
- Консольный интерфейс транслятора (входные и выходные файлы).
- Принципы работы транслятора (этапы: лексический анализ, парсинг S-exp в AST, генерация бинарного машинного кода).
-->

## Модель процессора

Процессор реализован как **5-стадийный конвейер** `IF → ID → OF → EX → MEM/WB` с **hardwired ControlUnit** (без микрокода). Конфликты данных по регистрам закрываются **форвардингом** из EX и MEM/WB; блокировки (stalls) остаются лишь для конфликтов по памяти и структурного конфликта D-Mem. Ветвления разрешаются на стадии **OF** (penalty взятого перехода — 2 такта); смена потока — сбросом (flush). Гарвардская архитектура исключает структурные конфликты между выборкой инструкций и доступом к данным.

Состав стадий продиктован CISC-операндами в памяти: арифметика может читать операнд из памяти (`ADD R0, [FP+2]`) и даже модифицировать ячейку (`ADD [addr], R0`, RMW). Поэтому операнд из памяти читается **до** ALU — на отдельной стадии **OF** (Operand Fetch: вычисление адреса + чтение D-Mem). Результат (в том числе в память) пишется **после** ALU, на объединённой стадии **MEM/WB** — команда пишет ровно одно назначение: регистр **или** ячейку памяти.

> Полное поэлементное описание датапаса (мультиплексоры, конвейерные регистры, сигналы) приведено в [docs/processor_schema.md](docs/processor_schema.md). Ниже — обзор.

### DataPath

![DataPath](docs/datapath.svg)

#### Стадии конвейера

**IF (Instruction Fetch).** Читает одно машинное слово из I-Mem по адресу `PC` и инкрементирует `PC`. Если команда многословная (слова расширения `imm32` при `S=0` для режимов `001`/`010`/`011`, либо доп. слово регистров/`func` у формата C1), Control Unit входит в состояние `FETCH_EXT` и дочитывает слова — по одному за такт, замораживая фронт. Пока команда собрана не полностью, в IF/ID выставлен `valid=0` и ID получает пузырь. Результат фиксируется в регистре IF/ID.

**ID (Instruction Decode).** Декодирует опкод и дескрипторы операндов, читает значения регистров-операндов из Register File (два порта чтения), извлекает непосредственные значения (`imm32` из слова расширения или `imm8` из младшего байта `[7:0]` — позиция `imm8` единая для всех форматов). RAW-зависимости по регистрам закрываются **форвардингом** (значения подменяются на стадиях OF и EX результатом из EX/MEM-WB), поэтому обычный RAW не стопорит конвейер; Hazard Detection Unit оставляет `Stall` лишь для конфликтов по памяти и структурного D-Mem. Здесь же в конвейер пробрасывается C1-payload (`count`, список регистров, адрес `func`) многотактных команд. Обращений к памяти данных и разрешения ветвлений на этой стадии нет. Результат фиксируется в регистре ID/OF.

**OF (Operand Fetch).** Вычисляет эффективный адрес (`Address Adder`: `base + offset` для режима `011`, абсолютный для `010`, `Rn` для постинкремента `100`) и, если операнд в памяти, читает D-Mem (один порт). К концу стадии операнд из памяти доступен как `MemOperand`. Для режима `[Rn]+` параллельный `PostInc Adder` готовит `Rn+1` для обратной записи. **Здесь же разрешаются ветвления**: `Branch Adder` считает `PC_next + offset24`, логика условия проверяет флаги `SR` (для соседнего `CMP` они пробрасываются из EX); при взятом переходе `Branch_Flush` очищает IF/ID и ID/OF (**penalty 2 такта**). Базовый регистр адреса при необходимости берётся форвардом из EX/MEM-WB. Результат фиксируется в регистре OF/EX.

**EX (Execute).** Оба операнда уже готовы (регистр, immediate, `MemOperand` или **форвард** из EX/MEM-WB). **ALU** выполняет арифметику (`ADD`, `SUB`, `MUL`, `DIV`, `MOD`), сравнение (`CMP` → флаги `Z`, `N` в `SR`), условное присваивание (`SETL`, `SETG`, `SETE`) и `PASS` (для `MOV`/`OUT`/store). В EX запускаются многотактные состояния Control Unit (`INVOKE`/`PUSHM`/`POPM`/`CALL`/`RET`/`IRET`) — к этому моменту старшие команды уже покинули датапас. Результат фиксируется в регистре EX/MEM-WB.

**MEM/WB (Memory Access + Write Back).** Объединённая стадия завершения. Команда пишет ровно одно назначение: регистр (порт W0) **или** ячейку D-Mem — они взаимоисключающи. `Result MUX` выбирает данные для записи в регистр: результат ALU (в т.ч. загруженное значение, прошедшее `PASS`), значение порта (`IN`) или слово из D-Mem (`POPM`). Постинкремент пишется параллельно через порт W1. Через порт D-Mem здесь идут только записи результата (store/RMW) и стековые обращения Control Unit. Запись в RF — по переднему фронту, чтение в ID — по заднему (write-before-read), что снимает RAW на дистанции 3 без stall.

#### Основные компоненты DataPath

| Компонент | Описание |
| --- | --- |
| **I-Mem** | Память команд (read-only). Содержит только код, адресуется по `PC`. |
| **D-Mem** | Память данных (read/write, однопортовая). Читается в OF, пишется в MEM/WB. Адрес — `Effective_Addr` или адрес стека от Control Unit. |
| **Register File** | 16 регистров (R0-R15, где R14=FP, R15=SP). 2 порта чтения (ID), 2 порта записи (W0 — результат, W1 — постинкремент/`SP`). |
| **ALU** | Арифметика, сравнения, SETcc, PASS. Входы: операнды из OF/EX. Выходы: результат, флаги Z и N. |
| **Address Adder** | Вычисление эффективного адреса в OF. Стековый адрес тоже считает он (`SP`/`SP−1`). |
| **PostInc Adder / SP_Adder** | Инкременторы «±1»: `Rn+1` для режима `[Rn]+` и обновление `SP` при стековых операциях. |
| **Branch Adder** | Считает `PC_next + offset24` для ветвлений (в OF). |
| **I/O Port Controller** | Port-mapped ввод-вывод (`IN`/`OUT`); источник запросов прерываний `IRQ`. |
| **PC** | Счётчик команд. Источник (MUX): `PC+1`, branch target (EX), адрес от Control Unit (возврат/вектор/CALL/INVOKE). |
| **SR** | Статусный регистр (`Z`, `N`, `IE`). Обновляется из ALU (CMP), `EI`/`DI` и аппаратно (прерывания). |
| **Pipeline Registers** | IF/ID, ID/OF, OF/EX, EX/MEM-WB — латчат данные и управляющие сигналы между стадиями. |

### ControlUnit

![ControlUnit](docs/controlunit.svg)

ControlUnit — единственный блок управления, без микрокода. Комбинационный **Main Decoder** задаёт сигналы для однотактного потока, а аппаратный автомат (**FSM**) добавляет многотактные команды и обслуживание прерываний. Многотактные команды переиспользуют общий датапас (тот же `Address Adder` для адреса стека, тот же порт D-Mem, те же порты RF); единственный выделенный элемент стека — маленький инкрементор `SP_Adder` (±1). Блок состоит из Main Decoder, Hazard Detection Unit и FSM с логикой ветвлений и прерываний.

#### Main Decoder

Получает опкод из стадии ID, генерирует управляющие сигналы, которые проходят через pipeline-регистры вместе с данными:

| Сигнал | Стадия | Описание |
| --- | --- | --- |
| `ALUOp[3:0]` | EX | Операция ALU: ADD, SUB, MUL, DIV, MOD, CMP, SETL, SETG, SETE, PASS |
| `ALUSrcSel` / `ALU_A_Src` | EX | Источник операндов ALU: регистр / imm / `MemOperand` (в т.ч. RMW) |
| `RegWrite` | MEM/WB | Разрешение записи в Register File (порт W0) |
| `MemRead` | OF | Разрешение чтения D-Mem (источник из памяти или RMW-приёмник) |
| `MemWrite` | MEM/WB | Разрешение записи D-Mem |
| `RegSrc[1:0]` | MEM/WB | Result MUX: ALU / порт / D-Mem |
| `Branch` / `BranchCond[2:0]` | OF | Ветвление и его условие: ALWAYS, EQ, NE, LT, GT |
| `IORead` / `IOWrite` | EX→MEM/WB | Чтение/запись порта (`IN`/`OUT`) |
| `IsMultiCycle` | EX | INVOKE/PUSHM/POPM — запуск состояния FSM |
| `IsCall` / `IsRet` / `IsIRET` | EX | Запуск состояний CALL/RET/IRET |
| `IsEI` / `IsDI` | EX | Установка / сброс `SR.IE` |
| `Halt` | — | Остановка конвейера |

#### Forwarding Unit и Hazard Detection Unit

**Forwarding Unit** пробрасывает результат «команды в полёте» прямо на входы потребителя, минуя запись в RF. Источники проброса — `EX/MEM-WB.ALU_Result` (после EX) и writeback-шина MEM/WB; приёмники — мультиплексоры операндов ALU (EX) и базы адреса (OF), а также флаги `SR` для соседнего `CMP→Bcc`. Это убирает RAW-stall по регистрам, включая load-use (значение из памяти готово уже после OF) и зависимость по базе адреса.

**Hazard Detection Unit** после добавления форвардинга отвечает лишь за остаточные случаи:

- **RAW по памяти (консервативно)**: эффективный адрес известен только в OF — если команда читает память при любой незавершённой записи впереди, вставляется `Stall` (проброс по памяти не делается).
- **Структурный конфликт D-Mem**: чтение в OF и запись в MEM/WB в один такт на единственный порт → `Stall` 1 такт, приоритет у MEM/WB.

#### FSM, ветвления и прерывания

**Однословный поток** идёт через Main Decoder без участия автомата. **Многотактные команды** запускают своё состояние, **когда доходят до EX** (к этому моменту старшие команды уже в MEM/WB или вышли — за D-Mem/RF никто не конкурирует), и замораживают фронт (`CU_Freeze`): `FETCH_EXT` (дочитка слов), `PUSHM`/`POPM` (стек регистров), `INVOKE` (push аргументов и `PC`, переход по `func`), `CALL`/`RET`/`IRET` (вызов/возврат через стек). Операнды этих команд (`count`, список регистров, `func`) пробрасываются по конвейеру в составе C1-payload и читаются автоматом из `OF/EX`.

**Ветвления.** Условие вычисляется на стадии **OF** по флагам `SR` (для соседнего `CMP` флаги пробрасываются из EX). При взятом переходе `Branch_Flush` очищает IF/ID и ID/OF (две ошибочно выбранные команды), `PC` загружается из `Branch Adder` (`PC_next + offset24`). **Penalty: 2 такта** на взятый переход, 0 — на невзятый (предсказание «не взято»). Команды, меняющие поток через автомат (CALL/RET/IRET/INVOKE), сами стоят в EX (`CU_HoldEX`) и сбрасывают две младшие команды позади (`CU_Flush` по IF/ID и ID/OF).

**Прерывания** (точные) проверяются на границе команд при `IE=1` и имеют наивысший приоритет:

1. Защёлкивается `Resume_PC` — адрес первой невыполненной команды.
2. `INT_Flush` сбрасывает **только** младшие стадии (IF/ID, ID/OF); команды в OF/EX и EX/MEM-WB **дотекают** до конца — это и обеспечивает точность прерывания.
3. На стек сохраняются `Resume_PC`, затем `SR`; `IE` сбрасывается в `0`.
4. `PC` загружается из таблицы векторов `D-Mem[vector_N]` (IVT хранится в памяти данных).
5. `IRET` снимает со стека `SR`, затем `PC` (а вместе с `SR` — и `IE`).

### Такты и производительность

Колонка «такты» — пропускная (added) такты с учётом конвейеризации, а не латентность прохождения всех пяти стадий.

| Тип инструкции | Пример | Тактов в конвейере |
| --- | --- | --- |
| Регистр-регистр | `ADD R0, R1` | 1 (полностью конвейеризировано) |
| Регистр + imm8 (S=1) | `ADD R0, #5` | 1 |
| Регистр + imm32 (S=0) | `ADD R0, #100000` | 2 (1 такт на extension word в IF) |
| Память-источник (S=1) | `MOV R0, [FP+2]` | 1 (чтение D-Mem в OF, конвейеризировано) |
| Память-источник (S=0) | `MOV R0, [FP+imm32]` | 2 (extension word в IF) |
| Taken branch | `BEQ label` | 3 (1 + 2 penalty за flush IF/ID, ID/OF) |
| Not taken branch | `BEQ label` | 1 |
| Безоперандные | `RET`, `HALT` | 1 |
| `CALL` / `RET` | `CALL label` | ≈ 3 (flush 2 младших + такты push/pop стека) |
| INVOKE (N args) | `INVOKE func, 3, R0, R1, R2` | ≈ N + 2 (push N аргументов + `PC`, переход) |
| RAW-зависимость по регистру | `MOV R0,#1` → `ADD R1,R0` | 0 (закрыто форвардингом) |
| RAW по памяти / структурный D-Mem | — | +1 stall |

## Тестирование

<!-- TODO: Описать:
- Краткое описание разработанных тестов.
- Ссылки на golden-тесты (hello, cat, hello_user_name, prob1 и др.).
- Пример использования разработанной инструментальной цепочки.
- Результаты работы CI, метрики (покрытие, lint и т.д.).
-->
